import Menu from '../../Menu/Menu';
import MenuLink from '../../Menu/MenuLink';

const Header = () => (
  <>
    <Menu>
      <MenuLink to="/jank">Zurück zur Spieleauswahl</MenuLink>
    </Menu>
  </>
);

export default Header;
